# Copy to ".streamlit/secrets.toml" for local development (Streamlit will auto-load it).
# Do NOT commit ".streamlit/secrets.toml".

DEEPSEEK_API_KEY = ""
DEEPSEEK_BASE_URL = "https://api.deepseek.com"
DEEPSEEK_MODEL = "deepseek-chat"

VOLC_ASR_APP_KEY = ""
VOLC_ASR_ACCESS_KEY = ""
VOLC_ASR_RESOURCE_ID = "volc.bigasr.sauc.duration"
VOLC_ASR_WS_URL = "wss://openspeech.bytedance.com/api/v3/sauc/bigmodel_async"

# App-level login (recommended on Community Cloud where "Require sign-in" may be unavailable):
LAB_DIARY_AUTH_MODE = "email_otp"
# Allow any email under these domains (comma-separated), e.g. "yourorg.com,dept.yourorg.com"
LAB_DIARY_ALLOWED_EMAIL_DOMAINS = ""
# Optional explicit allowlist (comma-separated). If set, only these emails can log in.
LAB_DIARY_ALLOWED_EMAILS = ""

# Alternative (no SMTP): shared password login
# LAB_DIARY_AUTH_MODE = "password"
# LAB_DIARY_SHARED_PASSWORD = ""
# Or store hash instead of plaintext (sha256 hex of the password)
# LAB_DIARY_SHARED_PASSWORD_HASH = ""

# Email OTP settings (SMTP)
SMTP_HOST = ""
# QQ 邮箱常用：465(SSL) 或 587(STARTTLS)
SMTP_PORT = "587"
SMTP_USER = ""
# 注意：这里填写 QQ 邮箱的「SMTP 授权码」，不是 QQ 登录密码
SMTP_PASSWORD = ""
SMTP_FROM = ""
# 587: STARTTLS
SMTP_USE_TLS = "1"
# 465: SSL（若使用 465，请设置 SMTP_USE_SSL=1 且 SMTP_USE_TLS=0）
SMTP_USE_SSL = "0"
# 连接超时（秒）
SMTP_TIMEOUT = "20"
# 调试输出（1 会把 SMTP 握手打印到日志；线上建议关掉）
SMTP_DEBUG = "0"

# Optional: also require platform sign-in (only works if Streamlit Cloud provides it)
LAB_DIARY_REQUIRE_SIGNIN = "0"
